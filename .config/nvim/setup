## install neovim
# download tar.gz
tar xvzf nvim-linux
cd nvim-linux
cp bin/nvim /usr/bin
cp -r share/nvim/ /usr/share/nvim

## for undo
mkdir -p ~/.config/nvim/undodir

## add this to .bashrc for git to work in vim term
export GIT_TERMINAL_PROMPT=1

## for ale
# python and cloudformation
pip3 install flake8 cfn-lint

## for fzf
# ripgrep
https://github.com/BurntSushi/ripgrep
# ctags
https://github.com/universal-ctags/ctags

## for coc
 install nodejs
 :CocConfig
 :CocInstall
go: (install gopls if not already)
terraform (install terraform-lsp)
dockerfile (dockerfile-language-server-nodejs)
bash (bash-language-server)

